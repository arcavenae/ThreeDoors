# Story 30.1: Linear GraphQL Client & Auth Configuration

## Status: Done (PR #446)

**Epic:** 30 — Linear Integration
**Priority:** P2
**Story Points:** 3
**Depends On:** Epic 7 (complete)
**FRs:** FR116, FR117

## Description

As a ThreeDoors user who uses Linear for project management, I want to configure Linear as a task source with my API key and team selection, so that my Linear issues appear as ThreeDoors tasks.

## Acceptance Criteria

1. **AC1:** A `LinearClient` struct in `internal/adapters/linear/client.go` wraps an HTTP client that authenticates via `Authorization: <api_key>` header
2. **AC2:** The client supports querying Linear issues with cursor-based pagination (fetching all pages automatically for teams with >50 issues)
3. **AC3:** Configuration in `config.yaml` accepts linear provider settings with api_key, team_ids, assignee, and poll_interval
4. **AC4:** Env var `LINEAR_API_KEY` takes precedence over config file `api_key` when both are set
5. **AC5:** `HealthCheck()` verifies API connectivity by querying the `viewer` field (authenticated user info)
6. **AC6:** GraphQL queries use typed Go structs for compile-time safety
7. **AC7:** Rate limit handling: respect `Retry-After` headers and implement exponential backoff

## Tasks

- [x] Create `internal/adapters/linear/` package directory
- [x] Define `GraphQLClient` interface and implement with raw HTTP + JSON
- [x] Define typed query/response structs for `issues`, `viewer`, and `workflowStates`
- [x] Implement cursor-based pagination wrapper
- [x] Implement `LinearConfig` parsing from config.yaml with env var override
- [x] Implement `HealthCheck()` via `viewer` query
- [x] Implement rate limit backoff
- [x] Write unit tests with mocked GraphQLClient interface
- [x] Write integration test with `httptest.NewServer` serving canned GraphQL responses
