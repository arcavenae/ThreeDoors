# Story 0.40: OAuth Package Test Coverage

## Status: Done (PR #491)

## Story

As a maintainer,
I want the `internal/core/connection/oauth` package to have at least 85% test coverage,
So that the OAuth device code flow and browser integration are well-tested and CI coverage gates pass.


---

## Background

The oauth package was at 73.1% coverage, below the 75% CI floor. Key gaps included zero coverage for `OpenBrowser`, and missing edge-case tests for `pollOnce` error paths and `StartDeviceCodeFlow` invalid JSON handling.

## Acceptance Criteria

- [x] Coverage for `internal/core/connection/oauth` is at least 85%
- [x] `OpenBrowser` has tests for empty URL, valid URL, and cancelled context
- [x] `pollOnce` edge cases covered: missing access_token, unparseable error/success responses, empty error code
- [x] `PollForToken` unexpected error code path covered
- [x] `StartDeviceCodeFlow` invalid JSON response path covered
- [x] All tests pass with `make test`
- [x] `make fmt` and `make lint` pass with zero warnings
- [x] Only test files modified — no changes to production code

## Tasks

- [x] Read existing code and identify coverage gaps
- [x] Write `browser_test.go` with table-driven tests
- [x] Add edge-case tests to `devicecode_test.go`
- [x] Verify coverage >= 85%
- [x] Run `make fmt` and `make lint`

## Results

- Coverage: 73.1% → 89.4%
- `OpenBrowser`: 0% → 66.7% (remaining uncovered: platform switch branches for non-current OS)
- `PollForToken`: 90.5% → 100%
- `pollOnce`: 76.7% → 90.0%
- `StartDeviceCodeFlow`: 85.7% → 89.3%
- `validateConfig`: 100% (unchanged)
- `NewClient`: 100% (unchanged)
