# Story 17.9: Simplify Sci-Fi Theme and Improve Modern Theme Contrast

**Status:** To Do
**Epic:** 17 — Door Theme System
**Depends on:** Story 17.7
**Blocks:** None

## User Story

As a user,
I want the Sci-Fi theme to feel futuristic without overwhelming my task text, and the Modern theme to clearly show which door is selected,
So that both themes are usable for daily task management.

## Acceptance Criteria

### Sci-Fi Theme

**Given** the Sci-Fi theme is selected
**When** three doors are rendered
**Then:**

- AC1: Shade rails reduced from 2 characters (`░░`) to 1 (`░`) on each side, or removed entirely — recovering at least 2-4 characters of content width per door
- AC2: Mid-bar separator (`╠══╪════╪══╣`) removed — content and `[ACCESS]` label share a single panel
- AC3: `[ACCESS]` label rendered below task text within the same content area (not in a separate panel)
- AC4: Minimum 15 characters of usable text width at `MinWidth` (reduced from current 18 if border overhead decreases)
- AC5: Double-line outer frame (`╔═╗║╚╝`) preserved — this is the core Sci-Fi identity
- AC6: Selected state still uses `▓` (or brighter shade) for visual distinction

### Modern Theme

**Given** the Modern theme is selected
**When** a door is selected vs unselected
**Then:**

- AC7: Selected door border color is noticeably brighter or uses a distinct hue (not just line weight change)
- AC8: Selected state uses color `255` (bright white) AND unselected uses a dimmer color (e.g., `240` instead of current `245`) to increase contrast gap
- AC9: Doorknob position adjusted — placed with less bottom padding to reduce bottom-heavy visual weight

## Quality Gate

- QG1: `make fmt` passes (gofumpt formatted)
- QG2: `make lint` passes with zero warnings
- QG3: `make test` passes — all tests green
- QG4: Rebased on latest main
- QG5: Scope-checked against this story — no extras
- QG6: Errors handled with context wrapping (`%w`)

## Technical Notes

### Sci-Fi Changes
- `scifi.go:44`: Change `railW` from 2 to 1 (or 0) — this reduces total border overhead from 8 to 6 (or 4)
- Remove the mid-bar `╠══╪════╪══╣` at line 92 and the lower control panel structure (lines 96-118)
- Move `[ACCESS]` label to the last content line, right-aligned or centered, within the single panel
- Update `MinWidth` from 18 to 15 if overhead allows

### Modern Changes
- `modern.go:15`: Change `frameColor` from `"245"` to `"240"` for dimmer unselected borders
- Consider adding a subtle accent: selected border could use color `"117"` (light blue) instead of plain `"255"` for more personality
- `modern.go:88-97`: Reduce bottom padding from 2 blank lines to 1, moving doorknob closer to content

## Tasks

1. Reduce Sci-Fi shade rail width and remove mid-bar separator
2. Restructure Sci-Fi to single content panel with `[ACCESS]` label inline
3. Update Sci-Fi `MinWidth` if border overhead decreased
4. Increase Modern selected/unselected color contrast
5. Adjust Modern doorknob vertical position (reduce bottom padding)
6. Regenerate golden files for both themes
7. Verify all quality gates pass

## Pre-PR Checklist

- [ ] `make fmt` — no formatting changes needed
- [ ] `make lint` — zero warnings
- [ ] `make test` — all tests pass
- [ ] `go test -race ./...` — no race conditions
- [ ] Sci-Fi: task text readable without visual noise competition
- [ ] Modern: selected door visually distinct at a glance
- [ ] Both themes: content width >= 15 chars at MinWidth
