# Story 35.7: Shadow/Depth Effect for 3D Door Appearance

## Status: Done (PR #239)

**Epic:** 35 — Door Visual Appearance
**Priority:** P2
**Depends On:** 35.2-35.5 (all merged), 35.6 (PR #238)

## User Story

As a user,
I want doors to have subtle shadow/depth effects on the right and bottom edges,
So that doors appear to have dimension and stand out from the background.

## Acceptance Criteria

1. **Given** any door theme rendering a door
   **When** the door is rendered at sufficient width (>= MinWidth + 2)
   **Then** a shadow column using half-block characters (`▐` or `░`) is rendered on the right edge of the door
   **And** a shadow row using half-block characters (`▄`) is rendered below the threshold line
   **And** the shadow creates a visual impression of depth/3D

2. **Given** the selected door state
   **When** a door is highlighted as selected
   **Then** the shadow effect is enhanced (brighter or thicker shadow) to lift the selected door visually

3. **Given** terminal width insufficient for shadow (< MinWidth + 2)
   **When** doors are rendered
   **Then** shadow is omitted gracefully (no layout breakage)

## Quality Gates

- AC-Q1: `make fmt` passes
- AC-Q2: `make lint` passes with zero warnings
- AC-Q3: Test coverage for shadow rendering
- AC-Q4: Rebased on main
- AC-Q5: Changes scoped to shadow/depth only

## Technical Notes

- Shadow characters from block elements range (U+2580-U+259F)
- Right shadow: `▐` (right half block) — appended to each row
- Bottom shadow: `▄` (lower half block) — row below threshold/floor
- Shadow adds 1 char width overhead + 1 row height overhead
- Selected doors get enhanced shadow (doubled or brighter character)
- Compact mode (height < MinHeight): no shadow applied
